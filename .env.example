# Compose
# ------------------------------------------------------------------------------
# Local (default): docker-compose.local.yml
# Production:      docker-compose.production.yml
# Public host:     docker-compose.production.yml:docker-compose.caddy.yml
COMPOSE_FILE=docker-compose.local.yml

DEBUG=true
# Signs the short-lived form-errors cookie. Set a random value in production.
SECRET_KEY=dev-insecure-change-me

# Database (PostgreSQL)
# ------------------------------------------------------------------------------
# An absolute path connects through PostgreSQL's Unix socket.
POSTGRES_HOST=/var/run/postgresql
POSTGRES_PORT=5432
POSTGRES_USER=hyperspace
POSTGRES_PASSWORD=hyperspace
POSTGRES_DB=hyperspace

# Caddy (Reverse Proxy)
# ------------------------------------------------------------------------------
DOMAIN=hyperspace.example.com
