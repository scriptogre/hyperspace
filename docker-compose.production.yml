services:
  spacetimedb:
    build: .
    container_name: spacetimedb
    expose:
      - "3000"
    environment:
      - SPACETIMEDB_DEFAULT_MODULE=hyperspace
    volumes:
      - hyperspace_stdb:/stdb
    restart: unless-stopped

volumes:
  hyperspace_stdb:
