# Hyperspace

Multiplayer isometric sandbox where players place and drag 3D blocks on a shared grid in real time.

**Stack:** SpacetimeDB (Wasm module) + Rocket (WebSocket server) + htmx (live DOM morphing) + Tailwind CSS (3D transforms)
